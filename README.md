In this repository, I have implemented an image hover effect for a web page. The main goal is to make an image disappear when the mouse pointer hovers over it and reappear when the mouse pointer leaves the area.

Two separate approaches are provided: JavaScript-only and no css (index.html) and CSS-only no javascript (css.html).
